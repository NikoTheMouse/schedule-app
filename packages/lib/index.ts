/**
 * @repo/lib — shared utilities.
 * Phase 1 ships stubs only (D-05); Phase 3 implements the real logic.
 */
export { formatDate, parseDateRange, eachDayInRange } from "./src/dates";
export { calculateOverlap, rankTopOverlaps } from "./src/overlap";
export type { OverlapMap } from "./src/overlap";
