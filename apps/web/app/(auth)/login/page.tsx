/**
 * Login page placeholder. Phase 2 will implement the real login form
 * (email + password via Supabase Auth — see AUTH-01, AUTH-02).
 */
export default function LoginPage() {
  return (
    <main>
      <h1>Log in</h1>
      <p>Login form ships in Phase 2.</p>
    </main>
  );
}
